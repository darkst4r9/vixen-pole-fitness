# Contact page is a mailto link, no form data capture

`/contact` links out via `mailto:` instead of collecting submissions through a form. This keeps the site fully static with no backend or third-party form service to maintain, at the cost of no lead data being captured server-side.
